% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/external.R
\name{plot_stadia}
\alias{plot_stadia}
\title{Plot a puberty stadia diagram}
\usage{
plot_stadia(
  data = pub.data,
  persons = unique(data$id),
  plotline = c(TRUE, FALSE, FALSE),
  type = c(TRUE, FALSE, FALSE),
  colors = c("blue", "green", "red"),
  overlay = FALSE,
  ovsex = "M",
  ref = pubertyplot::pub.ref.lines,
  title = "Tanner pubertal stages - Patient ",
  padid = TRUE
)
}
\arguments{
\item{data}{A data frame with columns \code{id}, \code{age}, \code{sex}
and the pubertal stage variables (\code{gen}, \code{phb}, \code{tv} for
males; \code{bre}, \code{phg}, \code{men} for females).}

\item{persons}{Vector of person identifiers (values of \code{data$id}) to
plot.}

\item{plotline}{Logical vector of length 3 indicating which reference
lines to draw.}

\item{type}{Logical vector of length 3 indicating which pubertal stage
variables to plot.}

\item{colors}{Character vector of length 3 with the colors used for the
three pubertal stage variables.}

\item{overlay}{If \code{TRUE}, plot all persons on a single graph.}

\item{ovsex}{Sex (\code{"M"} or \code{"F"}) used to select the reference
lines when \code{overlay = TRUE}.}

\item{ref}{Reference data used to draw the reference lines, as produced
by \code{\link{pub.ref.lines}}.}

\item{title}{Plot title.}

\item{padid}{If \code{TRUE}, append the person identifier to the title.}
}
\value{
Invisibly returns \code{NULL}; the function draws on the current
  graphics device.
}
\description{
Plots Tanner pubertal stage measurements for one or more persons against
the Dutch 1997 reference curves, expressed as standard deviation scores
(SDS) by age.
}
